import{learningSections,sources}from'../data/content.js?v=census-resources-v1';
const root=document.querySelector('#learning-content');if(root){root.replaceChildren(...learningSections.map(section=>{const wrap=document.createElement('section');wrap.id=section.id;wrap.className='learning-section';const header=document.createElement('div');header.className='learning-section-header';header.innerHTML=`<h2>${section.title}</h2><p>${section.intro}</p>`;const grid=document.createElement('div');grid.className='learning-grid';section.items.forEach(item=>{const source=sources[item.source];const card=document.createElement('article');card.className='learning-card';card.innerHTML=`<span class="scope-tag">${item.scope}</span><h3>${item.title}</h3><p>${item.body}</p><p class="source-line">Source: <a href="${source.url}" target="_blank" rel="noopener noreferrer">${source.publisher}: ${source.title} <span class="sr-only">(opens in a new tab)</span></a></p>`;grid.append(card)});wrap.append(header,grid);const back=document.createElement('a');back.className='back-top';back.href='#main';back.textContent='Back to top ↑';wrap.append(back);return wrap}))}
