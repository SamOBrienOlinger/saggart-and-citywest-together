const form=document.querySelector('#contact-form');const status=document.querySelector('#form-status');form?.addEventListener('submit',event=>{event.preventDefault();status.hidden=false;if(form.elements.company.value){status.textContent='Unable to validate this submission.';status.focus();return}if(!form.checkValidity()){form.reportValidity();status.textContent='Please complete every required field before continuing.';status.focus();return}status.textContent='Your message passed local validation. It was not sent or stored because this is the private-review demonstration.';status.focus()});
